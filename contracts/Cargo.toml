[workspace]
members = ["nullifier-registry", "escrow-milestone", "tree-escrow", "location-proof"]
resolver = "2"

[profile.release]
overflow-checks = true
lto = true
codegen-units = 1
panic = "abort"

[workspace]
members = ["donation-escrow", "nullifier-registry", "escrow-milestone", "tree-escrow", "location-proof", "naira-payout", "farmer-registry"]
resolver = "2"

[profile.release]
overflow-checks = true
lto = true
codegen-units = 1
panic = "abort"
